# Backups generated by install.sh. Anchored to the root on purpose: unanchored,
# `backup-*/` also matched skills/backup-recovery-standards/ and silently kept it
# out of the repository.
/backup-*/
memory/
# Environment noise
.idea/
.DS_Store
*.swp
